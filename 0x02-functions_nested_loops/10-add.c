#include "main.h"

/**
 * add - adds two numbers
 * @a: input parameter a
 * @b: input parameter b
 * Description: given two numbers and returns their sum
 * Return : sum.
 */
int add(int a, int b)
{
	return (a + b);
}
