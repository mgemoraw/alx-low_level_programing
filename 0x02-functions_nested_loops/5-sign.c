#include "main.h"

/**
 * _isalpha - checks alphabet characters
 * @n: contains value to be checked
 * Return: Always 0.
 */

int print_sign(int n)
{
	if (n >= 65 && n <= 122)
		return (1);
	else if (n == '0')

	else if (n < 0)
		return (-1);

}
