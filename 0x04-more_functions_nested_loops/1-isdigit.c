#include "main.h"

/* more headers goes there */

/**
 * _isdigit - function checks digit numbers
 * @c: input character
 * Return: Always 0
 */

int _isupper(int c)
{
	if (c >= 48 && c < 58)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
