#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
* cents - function prints number of args
* @s: input character
* Return: returns 0
* Author: mgemoraw
*/
int cents(int arg)
{
	if (argc != 2)
	{
		printf("Error\n");
		return (1);
	}

/**
* main - function prints number of args
* @argc: argument count
* @argv: argument array
* Return: returns 0
* Author: mgemoraw
*/

int main(int argc, char *argv[])
{
    int i, count = 0;
    char *s = argv[1];
    if (atoi(argv[1]))
    for(i = 0; *s != '\0'; i++)
    {

    }
}
