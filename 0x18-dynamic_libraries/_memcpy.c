#include "main.h"
/**
 * _memcpy - copies bytes to the memory block
 * @dest: destination string
 * @src: source string
 * @n: position
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	char *str;

	return (str);
}

