#include "main.h"
/**
 * _memset - returns sth
 * @s: input character array
 * @b: character
 * @n: input integer
 * Return: returns sth
 */
char *_memset(char *s, char b, unsigned int n)
{
	char *str;

	return (str);
}

