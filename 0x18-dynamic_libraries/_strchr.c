#include "main.h"
/**
 * _strchr - copies string of chars
 * @s: character array
 * @c: character
 * Return: returns character array
 */
char *_strchr(char *s, char c)
{
	char *str;

	return (str);
}

