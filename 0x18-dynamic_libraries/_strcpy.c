#include "main.h"
/**
 * _strcpy - copies string from origin to destination
 * @dest: destination string
 * @src: source string
 * Return: returns pointer to the string
 */
char *_strcpy(char *dest, char *src)
{
	char *str;

	return (str);
}

