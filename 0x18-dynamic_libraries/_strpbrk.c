#include "main.h"
/**
 * _strpbrk - returns first occurance in string
 * @s: character string
 * @accept: target chracter array
 * Return: returns string if exist NULL otherwise
 */
char *_strpbrk(char *s, char *accept)
{
	return (NULL);
}

